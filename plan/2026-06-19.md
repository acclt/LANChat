- [x] ui界面设置端口和数据库路径
- [x] 渲染思考过程、工具调用

```
`保存时不关闭面板而是显示成功消息并提示重启`这个调整成如果修改了端口和数据库路径提示`设置保存成功，需重启生效`并关闭面板
`端口` `数据库路径` `下载路径`如果没有更改过，打开设置时对应输入框填入默认值
`端口` `数据库路径` `下载路径`的输入框如果被用户清空了，保存时重新写入默认值
`数据库路径`写入数据库会出现鸡生蛋蛋生鸡的问题，所以需要写入到配置文件中，我记得自定义主题就是放在配置文件夹，这里新引入config.json配置文件，键名使用db_path
`端口`配置保存在数据库，这里需要注意启动顺序的问题，读一下数据库看看有没有配置别的端口再启动服务
`Android 端`无法多开根本没必要修改数据库路径，直接不渲染数据库路径配置项目
`web端`本来就有cli参数指定端口和数据库路径的功能，参数指定的端口和数据库路径拥有最高优先级
```
