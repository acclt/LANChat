# LANChat 二次开发需求文档

- 文档版本：1.0
- 日期：2026-08-27
- 状态：需求整理完成，待 Android 界面设计

## 1. 背景与目标

当前版本具备局域网设备发现、聊天和文件传输能力，但 Android 附件选择体验较简陋，桌面端和 Android 端均只能一次选择一个附件，同时缺少可靠的消息通知体验。

本次改造目标：

1. 重新设计 Android 聊天界面及附件选择体验。
2. 为 Android 增加图片、文件和应用的多选发送能力。
3. 保持 Windows 当前界面，仅补充多选附件与 Windows 原生通知。
4. 保留现有离线补发能力；重新上线后允许从头重发，不要求断点续传。

## 2. 已确认的产品决策

1. Android 输入栏采用类似 QQ 的底部双层布局，但不复制 QQ 的品牌资源。
2. 第一行由消息输入框和发送按钮组成。
3. 第二行只保留“图片”“文件”“App”三个等宽入口。
4. 不提供“其他选择”入口，也不保留隐藏的更多功能菜单。
5. 图片始终按原文件发送，不提供“原图”开关。
6. 点击“图片”后不跳转到独立页面，而是在当前聊天页底部展开半屏相册。
7. Windows 现有 UI 保持不变，不进行视觉重做。
8. 本次不修改现有局域网通信协议或安全模型。

## 3. Android 界面设计要求

### 3.1 设计基准

- 高保真设计稿以 412 × 915 逻辑像素为基准。
- 同时考虑不同屏幕宽度、横屏、软键盘和底部安全区域。
- 整体风格现代、克制、触控友好；重点优化聊天与附件发送效率。

### 3.2 默认输入栏

默认状态下，输入栏固定在聊天页面底部：

```text
┌──────────────────────────────┐
│ 消息输入框               发送 │
├──────────────────────────────┤
│ 图片         文件        App  │
└──────────────────────────────┘
```

交互要求：

- 消息输入框可随内容适度增高，但不能无限挤压消息区域。
- 无文字且无附件时，发送按钮显示为不可用状态。
- 当前打开的附件入口使用主题色高亮。
- 输入文字和选择附件可以在同一发送操作中完成。

### 3.3 半屏相册

点击“图片”后：

1. 收起软键盘。
2. 聊天消息区域保留在页面上半部分，并保持当前滚动位置。
3. 输入栏继续显示在聊天区与相册之间。
4. 页面下半部分展开应用内相册，占可用高度约 45% 至 50%。
5. “图片”入口保持高亮。

相册交互要求：

- 以响应式网格展示本机图片缩略图。
- 支持一次选择多张图片。
- 每张图片右上角显示选择圆圈。
- 选中后显示 1、2、3……的序号，表示发送顺序。
- 再次点击已选图片可取消选择，并自动重排后续序号。
- 点击图片主体可进入全屏预览。
- 支持切换相册目录。
- 底部显示“发送(n)”按钮，n 为当前选择数量。
- 不显示“原图”选项，始终发送原始图片文件。
- 关闭相册后暂时保留已选内容；重新打开后可以继续选择。
- 返回键第一次关闭相册，第二次才执行聊天页的返回行为。

异常状态必须有明确界面：

- 尚未授予图片访问权限。
- 相册为空。
- 缩略图加载中或加载失败。
- 图片已被移动、删除或无法读取。

为实现内嵌半屏相册，Android 端应使用 MediaStore 构建应用内图片选择器；不能依赖无法嵌入聊天页的系统全屏选择界面。

### 3.4 文件选择

- 点击“文件”后进入 Android 系统文件选择器。
- 支持一次选择多个文件。
- 返回聊天页后，以附件队列展示文件名、类型、大小和当前状态。
- 发送前允许移除单个文件。
- 多文件必须逐项显示等待、发送中、完成或失败状态。
- 单个文件失败不能阻止其他文件继续发送。

### 3.5 App 选择

- 点击“App”后显示本机可发送的已安装应用列表。
- 每项至少显示应用图标、应用名称和包名。
- 支持选择应用安装包并加入附件发送队列。
- 不应展示无法读取或明确不可导出的条目。
- 对拆分 APK、系统应用和受保护应用的处理方式列入待确认事项。

### 3.6 附件队列

- 图片、文件和 App 均进入统一附件队列。
- 队列应保留用户的选择顺序。
- 发送前允许预览和移除。
- 发送期间显示逐项进度及整体完成情况。
- 失败项提供重新发送入口。
- 选择大量附件时，界面仍需保持可滚动和可操作。

## 4. Windows 端要求

### 4.1 界面

- 保持当前 Windows UI、布局和视觉风格。
- 不进行整体界面重构。

### 4.2 多选发送

- 图片选择器支持一次选择多张图片。
- 文件选择器支持一次选择多个文件。
- 保留现有拖放文件、粘贴文件和粘贴图片能力。
- 多选内容进入发送队列，并显示每项进度和结果。
- 单个附件失败不能中断其他附件。

### 4.3 Windows 原生通知

必须增加或修复 Windows 原生消息通知：

- LANChat 不在前台或窗口最小化时，新消息触发系统通知。
- 通知显示发送者名称和消息摘要。
- 文件接收完成时触发通知。
- 点击通知后激活 LANChat，并打开对应聊天。
- 聊天列表显示未读状态。
- 用户打开对应聊天后清除该会话的未读状态。
- 提供通知总开关。
- 应用处于当前活动聊天且窗口在前台时，不重复弹出该聊天的新消息通知。

## 5. Android 通知

- Android 应用在后台时，新消息触发系统通知。
- 文件接收完成时触发通知。
- 通知显示发送者及消息摘要。
- 点击通知进入对应聊天。
- 适配 Android 13 及以上版本的通知权限。
- 通知权限被拒绝时，应用继续正常运行，并在设置页说明通知不可用。

## 6. 离线补发

保留现有离线补发能力，允许采用“重新发送”而不是“断点续传”：

1. 对方离线时，文本和附件进入待发送状态。
2. 对方重新上线后，自动重新发送待发送内容。
3. 文件从头开始发送，不要求从中断位置恢复。
4. 待发送状态必须持久化，应用重启后不能无故丢失。
5. Android 附件进入待发送状态时，必须确保 URI 或文件内容在稍后仍可访问；必要时复制到应用管理的持久缓存。
6. 界面明确区分“等待设备上线”“正在重新发送”“发送失败”和“发送完成”。
7. 用户可以手动取消尚未发送的队列项。

## 7. 兼容性与约束

- 保持 Rust、Tauri 2、HTML、CSS 和 JavaScript 的现有项目架构。
- 保持现有局域网自动发现、聊天历史、单文件发送和图片预览能力。
- 不破坏 Windows 拖放与剪贴板发送。
- 不因多选功能将所有附件一次性读入内存；大文件仍需流式处理。
- Android 继续遵守分区存储和 SAF/MediaStore 访问规则。
- 新增功能不得依赖互联网或云端服务。

## 8. 本次不包含的内容

- Windows 界面视觉重做。
- “其他选择”或更多功能面板。
- 图片“原图”开关。
- 文件断点续传。
- 群聊功能。
- 通信加密或鉴权改造。
- 云端同步或跨互联网传输。

## 9. 设计、开发与交付顺序

### 阶段一：Android 界面设计

1. 制作 412 × 915 Android 高保真设计稿。
2. 至少覆盖默认聊天、半屏相册、多选图片、文件队列、App 选择、离线待发送、通知与权限异常状态。
3. 制作独立可交互原型，不修改正式业务代码。
4. 由用户确认设计稿后进入开发。

### 阶段二：功能实现

1. 实现 Android 界面及多选附件。
2. 实现 Android 原生通知。
3. 为 Windows 增加多选附件。
4. 增加或修复 Windows 原生通知。
5. 保留并验证离线重新发送。

### 阶段三：构建与验证

1. 构建 Windows 定制程序。
2. 构建 Android APK。
3. 验证 Windows 与 Android 互传文字、单文件、多文件和多图片。
4. 验证前后台通知、点击通知跳转和未读状态。
5. 验证设备离线、重新上线和应用重启后的重新发送。
6. 定制构建产物输出到独立的 `dist-custom` 目录，保留原 v0.1.5 程序。

## 10. 验收标准

满足以下条件才视为完成：

- Android 默认输入栏只包含图片、文件和 App 三个附件入口。
- Android 半屏相册与聊天消息同时可见。
- Android 能一次选择并发送多张原始图片。
- Android 能一次选择并发送多个文件。
- Windows 能一次选择并发送多张图片或多个文件。
- 多附件逐项显示进度，单项失败不影响其他项目。
- Windows 与 Android 在后台时均能收到消息和文件完成通知。
- 点击通知能够打开正确聊天。
- 离线内容在设备重新上线后能够从头自动重新发送。
- 应用重启后待发送队列仍然存在。
- 现有聊天、历史记录、自动发现、拖放和剪贴板发送没有回归。
- Windows 与 Android 定制构建均成功生成并可启动。

## 11. 待确认事项

以下事项在设计或开发前需要进一步确定：

1. Android 图片和文件单次多选数量是否需要上限。
2. App 选择是否需要支持拆分 APK；若支持，采用 APKS 打包还是其他格式。
3. 是否允许选择系统应用。
4. 通知中是否完整显示消息内容，或仅显示“收到一条新消息”。
5. Android 设计是否同时提供浅色和深色两套主题。
