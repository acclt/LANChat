# LanChat v4：多设备通知推送、接收与 Windows 界面优化

- 文档版本：4.0
- 整理日期：2026-08-31
- 文档性质：增量设计与实施约束；不代表功能已实现或真机验收通过
- 适用范围：自有设备、可信局域网；Android 采集，Android / Windows 接收；参与设备同步升级
- 核心原则：先核对现状，再增量实现。保留现有后台生命周期与通信路径，不为通知功能重构稳定链路。

## 1. 本版方向与范围

用户手动启动 LanChat，在现有有效运行会话内，将允许 App 的系统通知推送给设置中勾选的设备。可以同时选择多台设备；每台设备独立处理，离线或发送失败只丢弃该设备对应的通知，不影响其他目标。

首页保留原有聊天设备列表，新增“信息推送设备”和“信息接收设备”栏目。栏目中的“设备名（信息推送）／设备名（信息接收）”是同一真实设备的不同业务入口，不是新的网络设备、服务器或连接通道。

> 本版范围：通知监听 + App 允许列表 + 多目标选择 + 推送／接收分类入口 + Windows 桌面布局优化 + 现有 `/ws` 上的 notification 消息 + Android / Windows 系统通知 + 接收端简单去重与防回环。

| 项目 | v3.2 | v4.0 |
| --- | --- | --- |
| 推送目标 | 单一目标 | 多选，每台独立发送 |
| 首页入口 | 以通知设置为主 | 原聊天栏目保留，增加推送和接收栏目 |
| 接收平台 | Android | Android 与 Windows |
| Windows UI | 不在主要设计范围 | 优化首页、设备导航、通知详情与设置，保持已有视觉风格 |
| 接收开关 | 未单独形成完整交互 | 接收端本地明确开启，默认关闭 |
| 通知记录 | 不进入聊天记录 | 独立只读视图，仅保留当前运行会话内的有限记录 |
| 离线策略 | 直接丢弃 | 保持；逐目标丢弃，不排队、不重试、不补发 |
| 生命周期、底层通信 | 保留现状 | 保持，禁止借本功能扩展 |

本文件替代 v3.2 中的单目标与仅 Android 接收设计。`updatev3.md` 保留为历史记录；冲突处以 v4 为准。本轮只编写升级文档，不实施业务代码、不提交 Git。

## 2. 审计结论与实施基线

### 2.1 工作区不能混淆

| 项目 | 已有审计记录 |
| --- | --- |
| 文档目录 | `D:\SoftwareFree\LANChat` |
| 文档目录基线 | `main`，此前审计提交 `cd3a301`，尚未包含完整后台架构 |
| 后台源码工作区 | `D:\SoftwareFree\LANChat-android-file-actions` |
| 后台分支 | `codex/android-background-receiving` |
| 后台此前审计 HEAD | `eeb4d06` |
| 已有未提交修改 | 此前审计发现，包括 Android 0.19 配置；实施前核对并保留 |

以上提交信息是前轮记录，不是本次重新验证的 APK 版本。本轮补充查看了 Windows 通知入口、本地依赖，以及现有 HTML / CSS 桌面布局；未构建或运行 Android / Windows 客户端。实际开发前必须核对后台工作区当前 HEAD、未提交改动和测试安装包，不在旧 main 上重新实现后台架构。

### 2.2 已有能力与复用方式

| 组件 | 现状与本版处理 |
| --- | --- |
| LanChatForegroundService | 当前 Android 会话及后台资源持有者；只增加通知处理分支和只读状态访问 |
| CoreRuntime | 进程级单例，管理网络资源；不改变初始化、启动和停止语义 |
| Discovery / PeerManager | 保留发现机制；复用真实设备 ID、当前地址和在线状态 |
| WebSocket `/ws` | 已有小消息通路，通常按次连接、发送后关闭；增加专用消息及同连接结果回复 |
| 文件传输 | 控制消息与 HTTP 文件上传保持现状，不修改文件核心及补发机制 |
| CoreEventBus | 复用当前 Service / 桌面消费与 UI 桥接；新增专用事件，不另建可靠事件系统 |
| Android 通知 | 在现有后台宿主中增加独立同步通知发布器，不混入聊天／文件通知 |
| Windows 通知 | 已有通知身份注册和 Toast 展示入口；需新增同步消息分支及同条通知更新适配 |
| UI | 原设备选择和聊天操作主要以 peer ID 区分；通知入口需额外区分用途，避免串入聊天 |
| Windows 布局 | 当前首页居中、最大宽度 680px，进入聊天后切为 300px 侧栏的双栏布局；优化为宽窗口下稳定双栏，减少切换跳动 |

Windows 当前 `show_windows_system_notification` 使用 `tauri-winrt-notification 0.7.2`。本地依赖的普通 Toast 展示接口没有直接暴露本功能所需的任意 tag / group 配置；进度条 tag 不应被用来伪装普通通知。因此“支持电脑接收”包含一个小型 Windows 发布适配器，不能仅增加 UI 就宣称完成。

现有 PeerManager 不是常驻连接管理器。“走原本路径”指复用发现、地址解析、`/ws` 和事件桥，不承诺复用一条长期连接。不得新增端口、服务器、Connection Manager 或虚拟 peer。

### 2.3 冻结边界

- Android 保持 `START_NOT_STICKY`、`stopWithTask=true`，划掉任务与显式退出继续按原逻辑停止。
- 不增加 BootReceiver、开机自启、系统回收恢复、无 UI 冷启动、`android_context.rs` 或第二套后台协调器。
- 不改 CoreRuntime 生命周期、Discovery、WakeLock / MulticastLock、UI 重建机制、SAF 保存链路和文件传输核心。
- 不改现有聊天／文件的存储、失败重发、通知抑制及清理规则。
- Windows 保留当前窗口、托盘、启动与退出行为；不增加 Windows 开机自启或后台守护进程。

## 3. 产品交互：设备仍是同一台，入口按用途区分

### 3.1 首页栏目

以 IQOO 向 REDM 和“我的电脑”推送为例，IQOO 首页显示：

~~~text
设备
  局域网聊天设备
    REDM                         在线
    我的电脑                     在线
    4060                         离线

  信息推送设备                   设置 >
    REDM（信息推送）              已开启 · 在线
    我的电脑（信息推送）          已开启 · 在线

  信息接收设备                   设置 >
    暂无接收记录
~~~

“我的电脑”开启接收并处理 IQOO 的通知后，其首页显示 `IQOO（信息接收）`。名称始终是远端设备：发送端显示目标，接收端显示来源。只有实际发生反向推送时，才在另一侧形成相应的反向入口。

- 聊天栏目继续用于聊天和文件，不改变原来设备条目的点击行为。
- 推送条目来自本机设置中的已选目标。目标离线仍保留选择及条目，显示离线；总开关关闭时显示“已暂停”，不清空选择。
- 接收条目由本机会话内首次成功处理的来源通知产生，不要求发送端同步配置，也不增加“创建接收通道”控制协议。
- 同一远端可同时出现在聊天、推送和接收栏目；底层仍只有一个真实 peer。
- 顶部“发现 N 台设备”继续统计真实设备，不把推送／接收条目再次计数。
- “已开启”表示本机配置，“在线”表示当前已有在线信息，二者不可混用。在线也不保证对方已开启接收。
- 来源名称优先取 PeerManager；找不到名称则显示 source_device_id，不依赖另外的名称查询协议。

示意图中的“只能选择 1 台”和单选圆点改为复选框及“已选 N 台”。示意图若将本机 IQOO 同时列作目标，应修正：按 device_id 排除本机，不以名称或 IP 判断。

### 3.2 设置页

Android 显示：

~~~text
信息推送
  信息推送总开关                 默认关闭
  推送目标设备（可多选）
    [✓] REDM                     在线
    [✓] 我的电脑                 在线
    [ ] 4060                     离线
  允许推送的应用                 默认全部未选择
  通知访问权限                   查看状态 / 前往系统授权
  发送测试通知                   发给当前勾选的目标

信息接收
  允许接收其他设备的通知         默认关闭
  系统通知权限                   查看状态 / 前往系统设置
~~~

Windows 首版只显示信息接收设置和系统通知设置入口，不展示不可用的 Android App 选择或系统通知采集开关。Android 仅接收时不需要通知访问权限，但需要系统允许 LanChat 发布通知。

推送与接收开关相互独立。关闭接收不影响自己的推送或聊天／文件；关闭推送不影响接收。原聊天通知开关不隐式控制本功能；本功能受自身接收开关和操作系统权限控制。

目标选择直接使用 PeerManager 列表，包含已有离线设备，不增加扫描机制。保存设备 ID 集合，不保存固定目标 IP。未知或暂不可达的已选 ID 保留，后续只有再次出现新通知时才按当前状态发送，绝不补发旧通知。不自动改投其他设备。

第一版所有目标共用同一份 App 允许列表，不增加每设备应用规则、时段规则、黑名单或优先级。

### 3.3 推送／接收详情

点击推送或接收条目打开独立的只读通知视图，显示来源 App、标题、正文、时间；推送视图还显示该目标的本次结果。没有聊天输入框、文件按钮、远程回复、历史补发按钮。

内部使用 `(peer_id, view_kind)` 区分入口，`view_kind` 为 `chat`、`notification_push`、`notification_receive`。真实设备资料仍引用原 peer，不伪造 `IQOO-push` 一类设备 ID。

只对入口分发、列表渲染与记录读取做必要隔离：通知条目不能进入 `loadChatHistory(peerId)`、正常消息发送、文件发送、聊天清空或原设备轮询清理逻辑。不得为了新增栏目重写整个聊天 UI。

通知记录仅保留当前有效运行会话，不写入原消息表，不新增历史数据库。第一版固定保留每个详情最多 50 条、全局最多 200 条最近展示记录，超限淘汰最旧记录；不做 UI 配置项。这些是只读展示记录，不是待发送队列。

记录和必要来源元数据由当前后台宿主在内存持有，UI 是只读投影；UI 重建时可取当前快照，UI 不可见不影响发送或系统通知。记录淘汰、用户关闭详情均不触发重发。会话停止清空记录；系统通知中心中已经发布的通知仍由系统按其规则管理。

### 3.4 Windows 首页与导航优化

Windows UI 优化是本版正式交付项，不只是为接收功能加一个设置开关。沿用当前浅色背景、紫色强调、圆角卡片及两端一致的用语，但按桌面窗口使用方式安排布局，不直接放大 Android 竖屏页面。

宽窗口采用固定导航区与弹性内容区，打开聊天或通知详情时不重新切换整页布局：

~~~text
┌────────────────────────┬───────────────────────────────────────────┐
│ LQ Chat          设置   │ IQOO（信息接收）                 接收设置 │
│ 我的电脑 · 本机地址     │ 来源在线 · 本机接收已开启                │
│ 添加设备               ├───────────────────────────────────────────┤
│                        │ 示例聊天                       今天 11:17 │
│ 局域网聊天设备         │ 张三                                      │
│   IQOO          在线   │ 晚上一起吃饭吗？                          │
│   REDM          离线   │                                           │
│                        │ 短信                           今天 11:12 │
│ 信息接收设备    设置 > │ 快递提醒                                  │
│ ● IQOO（信息接收）     │ 您的包裹已到达……                          │
│                        │                                           │
│ 正在监听局域网设备     │ 仅保留本次运行记录；此视图不能发送消息    │
└────────────────────────┴───────────────────────────────────────────┘
~~~

- 左栏约 300px，放置紧凑本机信息、添加设备、折叠设备分组及设置入口；地址采用次要文字，不让本机大卡片占满首屏。
- 右栏未选择条目时显示简洁欢迎／接收指引，不用空白聊天框或假的示例通知填充。
- 点击聊天设备进入现有聊天区，点击信息接收条目进入只读通知区；只切换内容，不重复创建窗口、WebView 或网络实例。
- Windows 首版仅接收 Android 通知，因此侧栏显示“局域网聊天设备”和“信息接收设备”，隐藏没有可用发送能力的“信息推送设备”。这不影响 Windows 原有聊天发送能力。
- 设备在线变化只更新相应状态，不重置当前选择、列表滚动或已展开通知；设备暂时离线仍可查看本次会话记录。
- 长名称省略并提供完整文本提示，IP 可选择复制；选中态、悬停态、键盘焦点态清晰区分，状态必须有文字，不能只靠颜色。

### 3.5 Windows 通知阅读与设置优化

通知详情采用紧凑卡片／列表，不模拟聊天气泡。顶部始终标明来源设备及“信息接收”，条目显示 App、标题、正文与时间；长正文可展开、折叠和选择复制。不新增附件动作、远程回复或打开手机 App 的按钮。

同条通知更新在原位置替换内容，保留阅读位置；用户正在向上浏览时不强制滚回顶部。新的通知可显示一个轻量“有新通知”入口，点击回到最新位置，不增加未读同步协议。

接收设置复用现有设置入口，以清晰分区呈现“允许接收其他设备的通知”、系统通知设置入口及“需先在手机选择本机”的说明。修改后立即显示保存结果；无系统权限查询能力时提供系统设置入口，不显示未经确认的“已授权”。

必须区分以下空状态，而不是统一显示“暂无消息”：

| 状态 | 页面提示与操作 |
| --- | --- |
| 接收未开启 | 本机尚未开启信息接收；提供开启入口 |
| 已开启但尚无记录 | 在 Android 的推送目标中勾选本机，并允许需要推送的 App |
| 来源离线 | 来源当前离线；保留本次运行记录，明确不会补收离线通知 |
| 系统权限已知受阻 | 提示系统通知可能无法显示，并提供设置入口，不自动改权限 |
| 会话重启记录清空 | 提示仅保存本次运行记录，不误报聊天历史被删除 |

首版不新增搜索索引、日期归档、持久未读数、跨设备已读同步或通知统计仪表盘。原聊天／文件详情仅调整与桌面外壳衔接必要的尺寸和间距，功能逻辑保留。

### 3.6 Windows 窗口与样式约束

- 内容区域宽度不小于 960 CSS px 时使用双栏；更窄时按列表／详情切换，保留明确返回入口，避免把两栏强行挤在一起。
- 导航与内容独立纵向滚动，标题与必要操作保持可见；长文本换行，不产生整窗横向滚动条。
- 保留原生标题栏、最小化／最大化／关闭、窗口拖动与托盘规则，不借 UI 优化改变退出行为。
- 在 1440×900、1024×768 和 800×600 窗口场景，以及 Windows 100%、125%、150% 显示缩放下检查布局；断点按实际 CSS 可用宽度判断。
- 支持 Tab 焦点移动、Enter / Space 操作按钮与开关，图标按钮提供文字标签；原有主题、语言入口继续可用。
- 复用现有 HTML / CSS / JS 与主题变量。新增布局规则限定 Windows 页面范围，不追加影响全部平台的全局覆盖，不引入新 UI 框架或重写聊天状态管理。
- 验收应提供 Windows 首页、通知详情、接收设置和窄窗口截图，并进行真实窗口操作检查；代码能编译不等于 UI 已优化完成。

## 4. 有效运行会话与原生设置

### 4.1 NLS 前置判断必须完全留在 JVM

双方都必须先手动启动 LanChat，且现有 Core 会话有效。Android 前台、Home 后台、锁屏与息屏仅沿用当前已验证的运行能力，不新增持续存活保证。

NotificationListenerService（下称 NLS）可能被系统单独绑定。此时仅有 App 进程或 NLS，不代表 LanChat 已运行。

- NLS 优先读取 LanChatForegroundService 在 JVM 内维护的只读“会话有效 / 原生已就绪”状态。
- 进程初始状态为 false；只在原有启动结果、Core 状态事件和退出处理中更新镜像，不持久化，不成为第二个生命周期管理器。
- 检查本地状态本身不能触发 `System.loadLibrary` 等原生加载。
- 不得先调用 `nativeIsCoreRunning()` 或任何 JNI 状态查询来判断 JNI 能不能用。
- 未就绪、状态未知或正在停止时直接忽略，不启动 Activity、Service、Core、网络任务，不保留通知等待启动。
- 本地门禁通过后才允许走已有原生桥。Rust 入口仍可防御性检查运行状态，处理检查后恰好停止的竞态；这不是前置 JNI 探测。

### 4.2 设置的唯一持久来源

Android 新增 `NotificationSyncSettings.kt`，使用 SharedPreferences 保存本功能配置，NLS 可直接读取。设置 UI 通过现有 Android 桥写入同一份设置，不在 Rust 数据库再保存一份竞争配置。

~~~json
{
  "push_enabled": false,
  "receive_enabled": false,
  "allowed_packages": [],
  "target_device_ids": []
}
~~~

UI 写入失败应回显失败，不能只改变前端开关。启动与 UI 重建时读取这份配置；Rust 发送时接收必要快照或参数，不要求 NLS 先进入 Rust 才能决定是否处理通知。

Windows 接收开关使用已有设置存储，默认 false；不引入 Android 设置文件，也不改现有存储结构为通用配置中心。

一次通知采用采集时的配置和目标集合快照，目标 ID 去重并排除本机。修改设置只影响后续事件，不撤回已经发出的副本。接收端在实际处理时检查当前接收开关。

## 5. Android 通知采集

NLS 只负责采集和过滤，不拥有 Socket、PeerManager 或 Core 生命周期。

处理顺序：JVM 本地门禁 → 排除 LanChat 自身包 → 推送开关与已选目标非空 → App 允许列表 → 标准进度通知过滤 → 提取并限长 → 调用已有原生桥。

- 仅支持 `EXTRA_TITLE`、`EXTRA_TEXT`、`EXTRA_BIG_TEXT`；正文优先使用非空 BIG_TEXT，否则 TEXT。
- 保留系统 notification_key、package 和 post_time；app_name 从本地应用资料读取，失败用 package。
- 标题和正文都为空时忽略。最小化数据，不读取通知图标、图片、附件或 PendingIntent。
- 不做 MessagingStyle、textLines、复杂分组语义、交互回复及撤回同步；系统移除原通知不要求远端同步移除。
- 标题最多 256 个 Unicode 字符、正文最多 4096 个 Unicode 字符、应用名最多 128 个 Unicode 字符；安全截断，不切断字符编码。
- NLS 回调只做轻量处理，网络 I/O 放入当前会话已有异步执行环境，不能阻塞系统回调。

**标准进度通知过滤（最新确认规则）：** A 端每次回调读取 `EXTRA_PROGRESS_MAX` 与 `EXTRA_PROGRESS_INDETERMINATE`；总量大于 0 或不确定进度标志为 true 时，直接忽略本次通知，包含开始、途中及 100% 的进度条通知。判断发生在图标处理、组包和 JNI 发送之前。不以字段是否存在、正文中的百分比／“下载”文字或常驻标志作为过滤依据。

原 App 清除进度条后发布的通知重新按普通规则处理；不维护开始／结束状态，不合成完成提醒，不因通知被移除而发送结束消息。不新增配置项、队列、节流或重试。缺少标准进度字段的自定义进度通知暂不猜测过滤；完成／失败时仍保留有效进度字段的通知也会被忽略。该规则同样适用于上传、安装等标准进度通知。

**A 不承担可靠去重，不维护内容指纹表。** 每个符合条件的系统通知事件都可以形成一次发送；最终去重与同条更新统一由接收端执行。只有实测出现大量重复回调后才另议小型发送节流。

## 6. 协议：多目标配置，逐目标单播

### 6.1 notification 消息

~~~json
{
  "msg_type": "notification",
  "event_id": "5b055be9-3c74-4ad5-a6d1-e2fbd786c412",
  "source_device_id": "device-iqoo",
  "target_device_id": "device-pc",
  "package": "com.example.chat",
  "app_name": "示例聊天",
  "title": "张三",
  "text": "晚上一起吃饭吗？",
  "notification_key": "0|com.example.chat|42|null|10001",
  "post_time": 1788153600000
}
~~~

| 字段 | 约束 |
| --- | --- |
| msg_type | 固定 `notification`，不冒充普通 text 消息 |
| event_id | 本次采集事件 ID；同一事件发往不同目标时可共用，不是内容去重依据 |
| source_device_id | 当前发送设备的既有 device_id |
| target_device_id | 此副本的唯一接收设备 ID；必须匹配接收方本机 ID |
| package / notification_key | 与 source_device_id 组成同条通知身份 |
| app_name / title / text | 展示内容；只作文本处理，不作为 HTML、命令或可执行动作 |
| post_time | 原系统通知时间，Unix 毫秒；不用于可靠排序或版本仲裁 |

`target_device_ids` 只存在设置中，线上每份消息仍然只有标量 `target_device_id`。勾选三台就是分别定向发送三份，不是局域网广播，也不是接收端再次转发。

notification JSON 按 UTF-8 序列化后最多 32 KiB；接收端验证字段类型、必填值、长度和目标 ID。不修改全局 WebSocket / HTTP 大小限制以免影响聊天及文件；超限通知在专用分支直接拒绝。

### 6.2 结果回复

~~~json
{
  "msg_type": "notification_result",
  "event_id": "5b055be9-3c74-4ad5-a6d1-e2fbd786c412",
  "target_device_id": "device-pc",
  "status": "success"
}
~~~

status 只有 `success` / `failure`。success 表示接收端已处理：系统发布 API 成功返回，或确认内容重复而忽略。它不保证横幅实际出现、用户看见或阅读。系统专注模式等仍由系统决定；已知接收关闭、权限不允许、数据非法或发布失败应返回 failure。

回复只回到当前请求的 WebSocket，不经过 `ws_broadcast`。发送端按 `(event_id, target_device_id)` 关联结果；错误目标、错误 event_id 的回复不能完成其他目标的请求。

如事件桥异步发布需要关联等待，只增加本次请求的短期结果等待对象，处理完成、断连、超时或停止即清理。不能据此新增持久回执、可靠收件箱或重试队列。

## 7. 多目标发送规则

~~~text
A 收到一条允许的通知
  → 本地运行门禁与设置检查
  → 取得本次目标 ID 集合
      ├─ REDM 在线：取当前地址 → /ws 一次发送 → 独立结果
      ├─ 我的电脑在线：取当前地址 → /ws 一次发送 → 独立结果
      └─ 4060 离线：丢弃该副本，保留目标选择
~~~

1. 每个目标发送前从 PeerManager 读取已有在线信息与当前地址。不固定 IP，不额外探活；在线只是允许尝试，最终仍可能失败。
2. 对当前可达目标分别发起独立异步请求，不等待第一台成功或超时才开始下一台。不得在网络等待期间持有 PeerManager、配置或整个发送器的全局锁。
3. 每个目标只尝试一次，连接、写入、等待回复采用一个总截止时间，首版固定 5 秒，不暴露给 UI；在现有执行环境中实现，不增加调度服务。
4. 离线、未知地址、连接失败、写入失败、超时、接收关闭或其他拒绝：该副本结束并丢弃。没有重试、离线队列、TCP 回退、聊天补发或目标替换。
5. 一个目标失败不取消其他目标。展示结果按目标记录，不将部分成功说成全部成功。
6. 首版全进程最多 16 个进行中的 notification 目标请求；无空位时立即丢弃新副本，不等待入队。容量计数只保护新通知请求，不改聊天／文件调度。
7. 有效会话结束时取消尚未完成的请求并清理内存，不为发送完通知延长生命周期。

超时可能发生在对方已提交系统通知但回复尚未到达时。因此 UI 可标记“超时”，不能断言对方绝未收到。即使如此也不自动重试。

发送记录最多表示一次尝试及其结果，不能被后台扫描重新发送。普通聊天／文件原有的离线和补发策略完全不受这套规则影响。

## 8. 接收、去重与系统通知

### 8.1 共同接收规则

接收方处理顺序：有效会话 → 本地接收开关 → 专用消息校验 → `target_device_id == 本机 device_id` → 来源不是本机 → 接收端去重与发布 → 返回结果。

第一版沿用现有 LAN 信任模型。开启接收表示允许当前 LAN 中使用该协议的发送者提交通知，不增加来源设备授权名单。关闭接收立即拒绝后续通知，不影响聊天和文件。

`source_device_id` 与 `target_device_id` 校验是路由及防回环检查，不是身份认证。即使接收方不能在 PeerManager 找到来源名称，也可在校验后用 ID 展示，不为此触发探活或能力协商。

同一个接收端可以同时接收多个来源。接收动作只能校验、记录和展示，不能进入发送流程；收到的 notification 不再转发给自己的已选目标。

### 8.2 接收端唯一去重状态

逻辑键为三元组 `(source_device_id, package, notification_key)`，比较内容仅包含 app_name、title、text：

| 情况 | 行为 |
| --- | --- |
| 新逻辑键 | 发布新系统通知，成功后记录内容 |
| 相同逻辑键、相同内容 | 忽略重复，返回 success，不重复弹出或新增接收记录 |
| 相同逻辑键、内容变化 | 更新原系统通知及现有接收记录，成功后替换内容 |
| 只有 event_id / post_time 变化 | 不构成内容更新 |
| 系统发布失败 | 返回 failure，不将失败内容记成已经成功展示 |

每个平台只有一个权威接收状态持有者：Android 为原生通知发布器，Windows 为同步通知发布适配器。Rust 公共路由层不再维护第二套内容去重表，UI 记录只是投影，不参与去重决策。

检查、发布和提交去重状态在各自发布器内按顺序处理，避免同条并发事件重复发布。最多保留 512 个逻辑键，淘汰最久未使用项；会话结束全部清空。淘汰后或重启后偶发重复可接受，不做跨进程去重、持久 event_id、revision、时序仲裁或 exactly-once 保证。

### 8.3 Android 发布

使用独立通知渠道，例如 `synced_notifications`，不复用聊天、文件或前台服务常驻通知的身份。

~~~text
tag = source_device_id + ":" + package + ":" + notification_key
id = 0
NotificationManager.notify(tag, id, notification)
~~~

同一逻辑键沿用固定 tag / id。不得将字符串 hash 为 int，也不得使用自增或随机 Android 通知 ID。字段仍按三元组存入去重表；按既有 device_id 格式验证来源 ID，避免拼接身份歧义。

内容显示“来源设备 · App”、标题和正文。不加载远端图标或执行远端动作。前台 UI 可见时也按本功能的接收设置处理，不复用原聊天的“当前窗口可见则不提醒”分支。

NLS 必须过滤 LanChat 自身 package，因此 B 展示的同步通知及 LanChat 普通聊天／文件通知不会再次被采集。本机来源 ID 拒绝和“不转发收到的 notification”共同防止回环。

**B 的 LanChat 已存在有效运行会话时，即使 UI 当前不可见，也能显示系统通知。** 不承诺 B 从未打开应用、已划掉退出或仅被系统绑定 NLS 时仍能接收。

### 8.4 Windows 发布

复用现有 Windows 通知身份注册与桌面 Core 事件消费入口，增加仅服务于 notification 的发布适配器；现有聊天／文件 Toast 辅助函数及其窗口焦点规则保持原样。

Windows 通过自身通知 API 展示，不使用 Android NotificationManager，不需要通知访问权限，也不增加 Windows 通知采集功能。现有进程按原规则在托盘运行时可接收，完全退出后不可接收。

Windows 的 tag / group 有平台长度限制，不能直接复制 Android 的完整字符串 tag。Microsoft 文档说明了 [ToastNotification.Tag](https://learn.microsoft.com/en-us/uwp/api/windows.ui.notifications.toastnotification.tag?view=winrt-26100) 与 [ToastNotification.Group](https://learn.microsoft.com/en-us/uwp/api/windows.ui.notifications.toastnotification.group?view=winrt-26100) 的限制。本版两者均控制在 16 字符以内。

首版采用会话内映射：发布器创建一次 16 字符会话 group，同一逻辑键首次出现时分配一个不超过 16 字符的计数 tag；后续更新复用这对 group / tag，调用原生 Toast API 重新提交同一身份的通知。映射与第 8.2 节去重记录合一，不另外持久化，计数不在同一会话复用；超出范围时拒绝新发布，不回绕。

该映射只承诺当前缓存和会话内更新。去重项淘汰或应用重启后，不追求与旧系统通知精确合并。必须验证通知中心同条替换行为，不能以新弹出一条代替更新验收。

## 9. 测试通知与状态展示

“发送测试通知”在当前有效运行会话内，读取同样的推送开关和多目标选择，使用同一 notification 发送入口、限长、并发上限、超时、接收开关及系统发布规则。按钮不另开连接协议，不做隐藏探活，不临时开启对方接收。

测试内容在本机直接构造，不先给 A 发布一条系统通知再依赖 NLS 采集。可使用固定合成 package `lanchat.test`、app_name `LQ Chat 测试`、notification_key `manual-test`，正文包含测试时间；这不是实际 App 通知，不要求加入 App 允许列表。仅本机显式点击生成，不能给普通 NLS 事件增加绕过允许列表的入口。

UI 逐目标显示“处理中 / 成功 / 失败 / 超时 / 离线已丢弃”等简短结果即可，不显示密钥、协议能力、TTL 或可靠性状态机。测试成功只说明本次处理成功，不永久标记设备支持协议或开启接收。

## 10. 通信隔离、隐私与性能

- notification 不进入普通 TextMessage、聊天数据库、未读聊天计数、文件控制流或离线补发。
- `notification` 和 `notification_result` 都不能进入 `ws_broadcast`，避免通知内容或回复被其他连接收到。
- 现有 `/ws` 转发器若独占写端，仅增加当前连接的直接回复入口；不要因此重构所有连接或重写消息总线。
- 增加专用 CoreEvent 及 UI 事件，接入已有唯一 Service / 桌面处理分支；不重复订阅导致一条通知发布两次。
- 复用 CoreEventBus，不新增待展示可靠队列。当前事件未能消费时让该请求失败或超时；不在 UI 上伪报成功。出现实测高负载丢失后再单独评估。
- Android 原生发布结果与 Windows 发布结果都回到当前请求，不要求 UI 存在、聚焦或完成渲染。
- 通知字段仅按文本呈现；JSON、XML 和 UI 文本使用正确转义，不拼接可执行内容。
- 通知标题、正文、完整原始帧不得写入调试日志；审查当前 `/ws` 原始消息日志，对新增类型在记录前脱敏。允许仅记录事件 ID、目标及简短错误。
- 沿用当前明文 LAN 信任模型；通知内容可能包含验证码、联系人等敏感信息，只应向自有可信设备开启。允许列表默认空、推送及接收默认关闭。
- 不增加配对密钥、Keystore、AES/HMAC、认证拒绝策略、重放保护或独立安全协议。
- 大文件并发先实测。只有通知延迟或原功能明显回退，才另案评估文件调度，不预先实现分片让出、降速或限流改造。

## 11. 最小代码落点与实施阶段

### 11.1 代码落点

以下路径相对于实际后台源码工作区；新增文件名可随既有命名习惯调整，职责与边界不能扩展。

| 位置 | 允许的增量 |
| --- | --- |
| AndroidManifest.xml | 声明 NLS 及必要权限；不改 task / Service 存活策略，不注册 BootReceiver |
| NotificationSyncSettings.kt（新增） | 原生配置及 UI 读写桥 |
| NotificationListenerService 实现（新增） | JVM 门禁、允许列表、采集与调用现有桥 |
| SyncedNotificationPublisher.kt（新增） | Android 接收开关、去重、稳定 tag / id、发布结果与记录快照 |
| LanChatForegroundService.kt | 暴露只读状态，已有事件回调内增加通知分支；不改生命周期 |
| src-tauri/src/android_service.rs | 复用现有 JNI / 事件桥，增加设置、发送及结果映射；无冷启动桥 |
| src-tauri/src/notification_sync.rs（新增） | 专用协议、按目标发送、校验、短期结果关联和发送记录 |
| src-tauri/src/network/messaging.rs | 复用 `/ws` 发送形式，通知专用单次请求与截止时间；不改聊天回退 |
| src-tauri/src/network/web_server.rs | 专用消息路由、同连接回复、通知日志脱敏 |
| src-tauri/src/core_events.rs | 新增 notification 专用事件，不能映射成普通 new-message |
| Windows 通知适配器（新增） | 复用通知身份；同步通知去重、tag / group、发布和接收记录 |
| src-tauri/src/main.rs / commands.rs / lib.rs | 在现有入口注册必要命令、设置和事件分支，不改 Core 创建与退出 |
| src/index.html、src/js/ui.js、src/js/app.js、src/css/style.css | 新增分类、复选目标、只读详情；Windows 稳定双栏、窄窗切换和设置布局；按用途及平台隔离，不改原聊天逻辑 |

Windows 接收设置可在现有设置层追加一个键，不新增通知历史表。不要把新增记录存入按 peer 查询的原消息表，否则通知会出现在普通聊天历史中。

### 11.2 Phase 1：配置、入口与采集

- 核对真实代码基线，记录现有聊天／文件及后台运行回归结果。
- Android 原生设置、JVM 门禁、NLS、允许 App 列表和通知访问入口。
- 首页推送／接收分类、目标复选、双方接收开关；未完成发送时不可显示虚假成功。
- Windows 导航／内容双栏外壳、桌面接收设置与各类空状态；保留原聊天组件及窗口行为。
- 验证本机不可选、离线选择保留、配置原生直读及 NLS 独立创建不进入 JNI。

### 11.3 Phase 2：多目标通路与双平台接收

- 专用 notification / result、现有 `/ws` 按目标发送与同连接回复。
- 各目标独立截止时间、容量满直接丢弃、无重试与聊天存储隔离。
- Android 与 Windows 发布器、接收端唯一去重、同条更新和防回环。
- 测试通知使用完全相同的通路；验证 Android → Android、Android → Windows 以及同时发送。

### 11.4 Phase 3：通知视图与完整回归

- 接收来源条目自动形成、发送结果逐目标呈现、会话内只读记录及 UI 重建快照。
- 完成 Windows 通知列表阅读体验、键盘操作、窗口缩放与窄窗适配；交付实际界面截图。
- 验证权限、接收关闭、超时、并发、退出清理与容量淘汰行为。
- 完成原有前台／后台／锁屏／息屏／UI 重建／多文件回归及 Windows 现有行为回归。
- 记录大文件并发时的实际表现；只有测出问题才提出单独优化方案。

## 12. 验收清单

| 类别 | 必须通过的验收 |
| --- | --- |
| 多选 | 同时勾选 Android B 与 Windows C，两端分别收到 A 的通知，设置保存 ID 集合 |
| 独立性 | B 离线、拒绝、连接挂起或不回结果，C 仍及时开始发送；不串行等待 B 的 5 秒截止时间 |
| 丢弃 | 目标恢复在线或接收重新开启后，不收到离线／失败期间旧通知 |
| 设置 | App 允许列表默认空；推送／接收默认关；原生可直接读取，不依赖 Rust 探测 |
| 本机排除 | 本机不能被选为目标；同名远端仍按不同 device_id 正确区分 |
| 地址变化 | 已选设备 IP 变化后，新通知使用 PeerManager 当前地址；离线不取消选择 |
| 进度过滤 | 标准进度 0%／中间／100%／不确定进度均不发送；清除进度条后恢复普通处理；普通百分比文本和无进度的常驻通知不因此误拦；不维护任务状态、不合成结束通知 |
| JVM 门禁 | 冷进程仅绑定 NLS，不触发原生加载、不调用 JNI、不启动 Core / Service、不缓存通知 |
| 停止竞态 | 门禁通过后会话停止，发送入口安全拒绝；不因退出中有通知而重新启动 |
| 接收条件 | 接收开关关闭、目标 ID 不匹配、来源为本机、数据非法或超限均不发布 |
| 多来源 | 不同 source_device_id 的同 package / notification_key 不互相覆盖 |
| 去重更新 | 同键同内容即使 event_id 变化也忽略；内容变化更新原系统通知及记录 |
| 单一状态 | Android / Windows 各仅一个接收去重持有者；并发相同事件不会重复发布 |
| 平台表现 | Android 固定 tag / id；Windows 当前会话同 group / tag 在通知中心更新原条目 |
| 权限 | 已知权限阻止或发布失败不回 success；重复忽略可回 success；不承诺实际横幅可见 |
| 防回环 | A / B 互选且均开启接收时，A 的原通知到 B 后不再返回 A 或转发第三台 |
| 测试通知 | 同样受本地会话、目标可达性、接收开关及权限约束；无另开协议或真实采集豁免 |
| 结果关联 | 相同 event_id 的多个目标结果不串线；回复不被其他 WS 客户端收到 |
| 容量 | 进行中请求与内存记录均有固定上限；容量满丢弃、不积压；退出后清空 |
| 首页 | 三类条目正确分组；发现数量只计真实设备；离线与已开启分别显示 |
| UI 隔离 | 通知详情不能发聊天／文件；普通聊天历史、输入框、未读与清空行为保持原样 |
| UI 不可见 | 双方已有有效会话时，Home、锁屏、息屏可同步；UI 重建不产生第二个 Core / 事件消费者 |
| 会话边界 | 划掉 Android 任务、显式退出、进程回收后不新增恢复；系统单独绑定 NLS 也不恢复 |
| Windows | 原窗口／托盘行为不变；进程有效时可收，完全退出不收；原聊天和文件通知无回退 |
| Windows 布局 | 宽窗首页、聊天与接收详情切换时侧栏稳定；窄窗返回正确；无新增不可用推送入口 |
| Windows 阅读 | 同条更新不打断滚动；长名称、长正文及大量会话记录不溢出；正文可选择复制 |
| Windows 设置 | 接收开关真实保存、错误有反馈；未开启、暂无记录、离线、权限受阻提示明确 |
| Windows 视觉验收 | 三种窗口尺寸与三档显示缩放下检查无遮挡、无横向溢出；Tab / Enter / Space 可用；提供页面截图 |
| 原链路回归 | 前台、Home 后台、锁屏、息屏及 UI 重建下的聊天、多文件、SAF 保存和原补发通过 |
| 并发回归 | 大文件传输同时推送到手机与电脑，记录通知延迟与文件表现；不预先改文件调度 |
| 持续运行 | 有效会话内至少 2 小时混合聊天、文件、通知，检查资源无持续增长、停止后锁及端口正常释放 |
| 隐私隔离 | 未选设备不收到通知；其他 WS 连接不收到原文／回复；日志和聊天库无通知正文 |

验收报告应分别注明代码检查、自动化检查和真机实测，不得用静态审阅替代“系统通知已显示”“锁屏接收稳定”或“Windows 同条更新”的运行证据。

## 13. 明确不做

开机自启、划掉 UI 后继续运行、START_STICKY、stopWithTask=false、BootReceiver、BOOT_COMPLETED、Direct Boot、无 UI 原生冷启动、系统自动恢复；新服务器、端口、连接管理器；配对、加密与新认证协议；离线队列、补发、复杂重试、展示可靠投递、跨崩溃去重；旧版能力协商；每目标 App 规则、黑名单；远程通知操作、回复与删除同步；Windows 系统通知采集；macOS / Linux 接收适配；持久通知历史；未经实测的文件调度重构。

这些事项不因“多选目标”“新增信息推送／接收入口”或“Windows UI 优化”自动进入范围。v4 的交付标准是：现有会话和现有网络路径保持稳定，在其上完成可多选、可向电脑发送、接收端独立展示的通知功能，以及适合桌面使用的 Windows 首页、通知详情与设置布局。
